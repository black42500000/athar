# Upstash Redis (from Vercel Marketplace)
# Install Upstash Redis from Vercel → Storage → Marketplace
# Vercel will auto-inject these env vars:
# KV_URL, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN

# Local development — no env vars needed (uses data/products.json)
